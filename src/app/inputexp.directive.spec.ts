import { InputexpDirective } from './inputexp.directive';

describe('InputexpDirective', () => {
  it('should create an instance', () => {
    const directive = new InputexpDirective();
    expect(directive).toBeTruthy();
  });
});
